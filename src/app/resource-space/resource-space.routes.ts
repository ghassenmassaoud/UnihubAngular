import { Route } from "@angular/router";
import { ResourceSpaceComponent } from "./resource-space.component";

export const Space_Route: Route[] = [
    {
        path:'spaces',
        component:ResourceSpaceComponent
    }

]